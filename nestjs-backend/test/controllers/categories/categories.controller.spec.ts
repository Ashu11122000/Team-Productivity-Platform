/* eslint-disable prettier/prettier */

describe('CategoriesController', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });

  it('should verify controller test setup', () => {
    expect('categories').toBe('categories');
  });
});